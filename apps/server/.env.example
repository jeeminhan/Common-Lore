# Server Configuration
PORT=3001
NODE_ENV=development

# CORS - supports comma-separated list for multiple origins
CORS_ORIGIN=http://localhost:3000

# For production, use:
# CORS_ORIGIN=https://crossings-card-game.pages.dev,http://localhost:3000

# Database (for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/crossings

# Redis (for production scaling)
# REDIS_URL=redis://localhost:6379
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxx
